---
- name: Export local security policy
  win_command: secedit /export /cfg C:\Windows\Temp\secpol.cfg

- name: Read exported security policy
  win_shell: type C:\Windows\Temp\secpol.cfg
  register: secpol_file

- name: Parse 'Profile system performance'
  set_fact:
    profile_system_raw: "{{ (secpol_file.stdout | regex_search('SeSystemProfilePrivilege\\s*=.*')) | default('SeSystemProfilePrivilege=') }}"

- name: Extract accounts into current list
  set_fact:
    current: "{{ profile_system_raw | regex_replace('.*=','') | trim | split(',') | map('trim') | list }}"

- name: Decide audit result
  set_fact:
    audit_result: >-
      {% set expected = ['*S-1-5-32-544','NT SERVICE\\WdiServiceHost'] %}
      {% if current | difference(expected) | length == 0 and expected | difference(current) | length == 0 %}
      PASSED
      {% else %}
      FAILED
      {% endif %}

- name: Display audit result
  debug:
    msg: "Audit result for 'Profile system performance' on {{ inventory_hostname }}: {{ audit_result }} (Current accounts: {{ current | default([]) }})"

- name: Add header row to CSV (only once)
  local_action:
    module: lineinfile
    path: /tmp/profile_system_audit.csv
    line: "Server Name,Timestamp,OS,Version,IPAddress,Env,Control,Impact,Result,Current_value,Expected_value"
    create: yes
    insertafter: BOF
  run_once: true

- name: Write audit result to CSV
  local_action:
    module: lineinfile
    path: /tmp/test.csv
    line: "{{ ansible_fqdn }},{{ now(fmt='%Y-%m-%d %H:%M:%S') }},{{ ansible_os_name }},{{ ansible_distribution_version }},{{ inventory_hostname }},{{ env }},Ensure 'Profile system performance' is set to 'Administrators, NT SERVICE\\WdiServiceHost',{{ impact }},{{ audit_result }},{{ current | join(';') | default('N/A') }},Administrators;NT SERVICE\\WdiServiceHost"
    create: yes

