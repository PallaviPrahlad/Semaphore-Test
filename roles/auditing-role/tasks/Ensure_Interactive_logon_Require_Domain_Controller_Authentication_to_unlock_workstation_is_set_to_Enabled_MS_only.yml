---
- name: Export local security policy
  win_command: secedit /export /cfg C:\Windows\Temp\secpol.cfg

- name: Read exported security policy
  win_shell: type C:\Windows\Temp\secpol.cfg
  register: secpol_file

- name: Parse 'Interactive logon Require Domain Controller Authentication to unlock workstation'
  set_fact:
    dc_auth_unlock_raw: "{{ (secpol_file.stdout | regex_search('ForceUnlockLogon\\s*=.*')) | default('ForceUnlockLogon=') }}"

- name: Extract current values into list
  set_fact:
    current: "{{ dc_auth_unlock_raw | regex_replace('.*=','') | trim | split(',') | map('trim') | list }}"

- name: Decide audit result
  set_fact:
    audit_result: >-
      {% if '1' in current %}
      PASSED
      {% else %}
      FAILED
      {% endif %}

- name: Display audit result
  debug:
    msg: "Audit result for 'Interactive logon: Require Domain Controller Authentication to unlock workstation' on {{ inventory_hostname }}: {{ audit_result }} (Current values: {{ current | default([]) }})"

- name: Add header row to CSV (only once)
  local_action:
    module: lineinfile
    path: /tmp/test.csv
    line: "Server Name,Timestamp,OS,Version,IPAddress,Env,Control,Impact,Result,Current_value,Expected_value"
    create: yes
    insertafter: BOF
  run_once: true

- name: Write audit result to CSV
  local_action:
    module: lineinfile
    path: /tmp/test.csv
    line: "{{ ansible_fqdn }},{{ now(fmt='%Y-%m-%d %H:%M:%S') }},{{ ansible_os_name }},{{ ansible_distribution_version }},{{ inventory_hostname }},{{ env }},Ensure 'Interactive logon: Require Domain Controller Authentication to unlock workstation' is set to 'Enabled' (MS only),{{ impact }},{{ audit_result }},{{ current | join(';') | default('N/A') }},Enabled"
    create: yes

